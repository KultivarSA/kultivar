<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- F7 — voice notes (AudioRecorder via `record` package) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!--
        Bug #171 — Notifications were dead on Android 13+ for the entire
        pre-launch window.  flutter_local_notifications calls
        requestNotificationsPermission() at boot, but Android 13+ rejects
        the runtime request silently if the permission isn't declared in
        the manifest first.  All care reminders (water, feed, IPM, burping,
        drying, target harvest) plus env-condition alerts depend on this.

        VIBRATE is paired with POST_NOTIFICATIONS because every channel we
        create requests vibration; without it Android downgrades the
        notification to silent-only on some OEMs.

        SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM are deliberately NOT
        declared -- all schedules in NotificationService use
        AndroidScheduleMode.inexactAllowWhileIdle, which works without
        the exact-alarm permission and keeps us off Play Console's
        "needs exact alarm justification" review queue.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!--
        RECEIVE_BOOT_COMPLETED lets flutter_local_notifications reschedule
        pending notifications after a device reboot.  Without it, every
        scheduled reminder is silently dropped when the user restarts
        their phone -- bad for a grow-tracker app where reminders are the
        core daily-engagement loop.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="Kultivar"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:dataExtractionRules="@xml/backup_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ── Kultivar glance widget ──────────────────────────────────── -->
        <receiver
            android:name=".KultivarHomeWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <!-- home_widget triggers a re-draw when Flutter calls updateWidget() -->
                <action android:name="es.antonborri.home_widget.action.UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kultivar_widget_info" />
        </receiver>

    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!--
            url_launcher needs to see installed mail handlers before it
            can resolve a `mailto:` URI on Android 11+ (API 30+).  Without
            this query block, `canLaunchUrl(mailto:...)` returns false
            even when Gmail / Outlook are installed; the OS hides
            those activities from the package manager unless we declare
            the intent we plan to dispatch.  Used by the Email Support
            tile in Settings (lib/screens/settings_screen.dart).
        -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
